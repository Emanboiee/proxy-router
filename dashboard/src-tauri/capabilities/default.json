{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capabilities for the Proxy Router dashboard window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
