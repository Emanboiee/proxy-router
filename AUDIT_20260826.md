# Proxy-router FULL AUDIT — 2026-08-26 (live-log root cause session)

## Goal
User reports: "can't stop via tray, profiles don't work, many things broken and unstable."
Full audit requested. Notebook v3 training runs in parallel — don't touch it.

## VERIFIED FINDINGS (from live logs + launchd + sudoers, 13:19-13:45 +07)

### F1. Elevated reload denied — sudoers PATH MISMATCH [FIXED]
- keepalive log: endless `ensure failed: elevated reload denied` loops w/ backoff 30→60→120s
- Root cause: `/etc/sudoers.d/91-proxy-router` grants NOPASSWD on
  `/opt/anaconda3/bin/python3 /Users/kyson/.local/bin/proxy-router <cmd>` (symlink path)
  but router/tray elevation used `/Users/kyson/proxy-router/router.py` (real path)
  → sudo exact-string match failed
- Fix: shared `_sudoers_router_path()` now resolves the installed alias only when it
  points to this source tree; router reload/doctor/elevate and tray sudo calls agree.
- Live proof: exact passwordless reload succeeded; final doctor says `elevate ... active`.

### F2. Tray dead + quit-spam "engine stop failed" [FIXED]
- Old log showed a deleted `examples/proxy_tray.py` path and repeated config-load failures;
  current plist points at root `proxy_tray.py` and is resident.
- Fixes: root-default calculation corrected; sudo alias used for root-owned stop/start;
  action timeout raised from 20s to 120s (router probe settle can be 60s); selftest now
  returns failure when a menu contract check fails.
- Live proof: tray process resident; tray-equivalent profile switch returned rc=0 in 64.7s
  and marker stayed on the requested profile.

### F3. sing-box NOT running while keepalive runs [FIXED]
- Historical state had keepalive alive with no engine. Current live state: sing-box
  PID 24322 listening on `127.0.0.1:2080`; KeepAlive and tray both resident.
- Live proof: `doctor` 8 ok / 0 warn / 0 fail; proxy-pinned OpenCode returned HTTP 200.

### F4. Historical ghosts and config-state traps [FIXED/CONTAINED]
- Reciprocal fallback cycle was real: `cloudflare → proton → cloudflare`.
  Current graph is acyclic: `cloudflare → proton → proton2`.
- Preset/fallback writers now validate the entire graph before writing, so a bad
  preset cannot poison router.json again.
- Old plist path was historical; current plist uses root `proxy_tray.py`.
- Home mode is deliberately narrow: `routing.mode=vpn-list`, `vpn_domains=["opencode.ai"]`.
  Roblox route remains available but is not active.

### F5. Dirty working tree with uncommitted fixes [KNOWN]
- Changes are intentionally left uncommitted on feature branch
  `fix/429-probe-classification`; no main/release merge was performed.
- Scope includes router lifecycle/elevation/cooldown/sweep fixes, tray fixes, watcher
  fixes, setup validation, and focused regression tests.
- Verification: `py_compile` clean, `bash -n examples/keepalive.sh` clean,
  full suite **511 passed**.

## State snapshot
- branch: `fix/429-probe-classification` (uncommitted by design)
- tests: **511 passed**
- sudoers file exists; alias path is used consistently by router + tray
- engine: sing-box PID 24322, proxy listener `127.0.0.1:2080`
- tray: launchd resident, PID 97160
- keepalive: launchd resident, PID 40518; persistent sweep cadence enabled
- watcher: persistently disabled for current home/Codex mode; targets report only `opencode.ai`
- home routing: vpn-list with `opencode.ai` only; Roblox route defined but inactive
- stale `opencode-deepseek-200-watch` Hermes cron: paused
- notebook v3 training: untouched by this audit

## Fix plan / verified outcome
1. F1 sudoers alias boundary — **DONE**, exact passwordless reload proof
2. F2 tray root path/timeout/sudo stop — **DONE**, tray-equivalent switch rc=0 in 64.7s
3. F3 engine recovery — **DONE**, live listener + OpenCode HTTP 200
4. Fallback-cycle writer guard — **DONE**, 2-cycle regression tests
5. Home vpn-list narrowing — **DONE**, only opencode rule generated
6. Watcher domain filtering + persistent off — **DONE**, survives reload/switch
7. KeepAlive boot/sweep churn — **DONE**, two-strike boot + persistent sweep timestamp
8. Interrupted sweep rollback — **DONE**, regression test restores original marker
9. Full verification — **DONE**, 511 tests + py_compile + bash -n + sing-box check

## Status
- [x] Live diagnosis complete
- [x] F1 fix + test
- [x] F2 fix + tray relaunch
- [x] F3 verification
- [x] Config cycle prevention
- [x] Home routing scope verified
- [x] Supervisor/watchers stabilized
- [ ] Commit + PR (not requested; branch remains uncommitted)
